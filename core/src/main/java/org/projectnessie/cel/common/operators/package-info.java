/**
 * Canonical CEL operator names and lookup metadata.
 *
 * <p>This package is primarily useful to parser, checker, macro, and extension implementations.
 * Application code normally writes operators in CEL source rather than referring to these constants
 * directly.
 */
package org.projectnessie.cel.common.operators;
